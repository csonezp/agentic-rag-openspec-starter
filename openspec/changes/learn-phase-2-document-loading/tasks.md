## 1. 设计与测试

- [x] 1.1 创建 OpenSpec change，明确文档加载学习目标、边界和验收标准。
- [x] 1.2 先编写 Markdown 文档加载测试，并确认测试失败。

## 2. 实现

- [x] 2.1 新增 `KnowledgeDocument` 文档对象。
- [x] 2.2 新增 Markdown 文档加载器，递归发现并读取 `*.md` 文件。
- [x] 2.3 实现标题提取、相对路径元数据和稳定排序。
- [x] 2.4 新增文档加载演示脚本，输出加载摘要。

## 3. 文档与验证

- [x] 3.1 新增学习笔记，记录文档加载边界和后续衔接点。
- [x] 3.2 运行单元测试和 OpenSpec 严格校验。
- [x] 3.3 使用 `knowledge/` 完成本地文档加载演示验证。
- [x] 3.4 完成后更新 `tasks.md` 和 `docs/agent-learning-todo.md` 勾选状态。
